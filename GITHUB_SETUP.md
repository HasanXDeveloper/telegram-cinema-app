# 🚀 Инструкция по загрузке проекта на GitHub

## 📋 Пошаговая инструкция

### 1. Создание репозитория на GitHub

1. **Перейдите на GitHub.com** и войдите в свой аккаунт
2. **Нажмите "New repository"** (зеленая кнопка)
3. **Заполните данные:**
   - **Repository name**: `telegram-cinema-app`
   - **Description**: `🎬 Telegram Cinema Mini App - полноценный онлайн-кинотеатр в Telegram`
   - **Visibility**: `Private` (рекомендуется для коммерческого проекта)
   - ❌ **НЕ ставьте галочки** на "Add a README file", "Add .gitignore", "Choose a license"
4. **Нажмите "Create repository"**

### 2. Подключение локального репозитория к GitHub

Выполните команды в терминале (в папке проекта):

```bash
# Добавляем удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/telegram-cinema-app.git

# Переименовываем главную ветку в main (современный стандарт)
git branch -M main

# Загружаем код на GitHub
git push -u origin main
```

### 3. Альтернативный способ через SSH (более безопасный)

Если у вас настроен SSH ключ:

```bash
git remote add origin git@github.com:YOUR_USERNAME/telegram-cinema-app.git
git branch -M main
git push -u origin main
```

### 4. Настройка SSH ключа (если нужно)

```bash
# Генерируем SSH ключ
ssh-keygen -t ed25519 -C "your.email@example.com"

# Копируем публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавляем ключ в GitHub:
# Settings → SSH and GPG keys → New SSH key
```

## 🔧 Команды Git для работы с проектом

### Основные команды:

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "✨ Описание изменений"

# Загрузить на GitHub
git push

# Скачать изменения
git pull

# Посмотреть историю
git log --oneline
```

### Работа с ветками:

```bash
# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на ветку
git checkout main

# Слить ветку
git merge feature/new-feature

# Удалить ветку
git branch -d feature/new-feature
```

## 📁 Структура коммитов

Используйте эмодзи для красивых коммитов:

```bash
git commit -m "✨ Добавлена новая функция"
git commit -m "🐛 Исправлен баг с авторизацией"
git commit -m "🎨 Улучшен дизайн главной страницы"
git commit -m "📝 Обновлена документация"
git commit -m "🚀 Подготовка к деплою"
git commit -m "🔧 Настройка конфигурации"
git commit -m "♻️ Рефакторинг кода"
git commit -m "⚡ Улучшена производительность"
```

## 🔒 Безопасность

### Что НЕ загружать на GitHub:

- ❌ Файлы `.env` с реальными ключами
- ❌ Пароли и токены
- ❌ Приватные ключи
- ❌ База данных с реальными данными
- ❌ Логи с чувствительной информацией

### Что можно загружать:

- ✅ Исходный код
- ✅ Файлы `.env.example` с примерами
- ✅ Документацию
- ✅ Конфигурационные файлы
- ✅ Docker файлы

## 🌟 Дополнительные настройки GitHub

### 1. Настройка GitHub Pages (для документации):

1. **Settings** → **Pages**
2. **Source**: Deploy from a branch
3. **Branch**: main → /docs
4. **Save**

### 2. Настройка защиты ветки main:

1. **Settings** → **Branches**
2. **Add rule**
3. **Branch name pattern**: `main`
4. ✅ **Require pull request reviews**
5. ✅ **Require status checks**

### 3. Создание Release:

```bash
# Создать тег
git tag -a v1.0.0 -m "🎬 Первый релиз Cinema App"

# Загрузить тег
git push origin v1.0.0
```

Затем на GitHub: **Releases** → **Create a new release**

## 🤝 Совместная работа

### Для команды разработчиков:

1. **Settings** → **Manage access**
2. **Invite a collaborator**
3. Добавьте GitHub username коллег

### Workflow для команды:

```bash
# 1. Создать ветку для фичи
git checkout -b feature/user-profile

# 2. Внести изменения и коммит
git add .
git commit -m "✨ Добавлен профиль пользователя"

# 3. Загрузить ветку
git push origin feature/user-profile

# 4. Создать Pull Request на GitHub
# 5. После ревью - слить в main
```

## 📊 GitHub Actions (CI/CD)

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Deploy to server
      run: |
        echo "Деплой на продакшн сервер"
        # Ваши команды деплоя
```

## 🎯 Готовые команды для копирования

Замените `YOUR_USERNAME` на ваш GitHub username:

```bash
# Полная настройка (выполнить один раз)
git remote add origin https://github.com/YOUR_USERNAME/telegram-cinema-app.git
git branch -M main
git push -u origin main

# Ежедневная работа
git add .
git commit -m "✨ Ваше описание изменений"
git push
```

## 🆘 Решение проблем

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/telegram-cinema-app.git
```

### Ошибка: "failed to push some refs"
```bash
git pull origin main --allow-unrelated-histories
git push origin main
```

### Забыли добавить .gitignore
```bash
git rm -r --cached .
git add .
git commit -m "🔧 Обновлен .gitignore"
```

---

**🎉 Готово! Ваш проект теперь на GitHub и готов к совместной разработке!**