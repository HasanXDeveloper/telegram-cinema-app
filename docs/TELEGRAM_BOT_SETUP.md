# Настройка Telegram бота для Cinema

## Создание бота

### 1. Создание через @BotFather

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите название бота: `Cinema Bot`
4. Введите username бота: `cinema_your_name_bot` (должен заканчиваться на `bot`)
5. Сохраните полученный токен

### 2. Настройка бота

```bash
# Установка описания
/setdescription
🎬 Ваш персональный кинотеатр в Telegram! Тысячи фильмов и сериалов с удобным просмотром прямо в мессенджере.

# Установка краткого описания
/setabouttext
Онлайн-кинотеатр в Telegram Mini App

# Установка команд
/setcommands
start - 🎬 Запустить кинотеатр
help - 🆘 Помощь и справка
stats - 📊 Моя статистика

# Загрузка аватара (опционально)
/setuserpic
```

### 3. Настройка Web App

После создания бота выполните:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setChatMenuButton" \
-H "Content-Type: application/json" \
-d '{
  "menu_button": {
    "type": "web_app",
    "text": "🎬 Кинотеатр",
    "web_app": {
      "url": "https://cinema.horizonserver.space"
    }
  }
}'
```

## Настройка переменных окружения

### 1. Создайте файл `.env`

```bash
cp telegram-bot/.env.example telegram-bot/.env
```

### 2. Заполните переменные

```env
# Токен бота от @BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrSTUvwxYZ

# URL вашего Web App
WEBAPP_URL=https://cinema.horizonserver.space

# ID администраторов (ваш Telegram ID)
ADMIN_IDS=123456789,987654321

# Уровень логирования
LOG_LEVEL=INFO
```

### 3. Как узнать свой Telegram ID

Отправьте сообщение боту [@userinfobot](https://t.me/userinfobot) или [@getmyid_bot](https://t.me/getmyid_bot)

## Запуск бота

### Локально (для разработки)

```bash
cd telegram-bot
pip install -r requirements.txt
python bot.py
```

### Через Docker

```bash
# Добавьте переменные в основной .env файл
echo "TELEGRAM_BOT_TOKEN=your-token-here" >> .env
echo "ADMIN_IDS=your-telegram-id" >> .env

# Запустите все сервисы
docker-compose up -d
```

## Функции бота

### Для пользователей

- **🎬 Запуск кинотеатра** - основная кнопка Web App
- **📊 Статистика** - персональная статистика просмотров
- **💬 Обратная связь** - отправка сообщений администраторам
- **🆘 Помощь** - справочная информация
- **ℹ️ О проекте** - информация о Cinema

### Для администраторов

- **📊 Статистика** - общая статистика бота
- **📢 Рассылка** - отправка сообщений всем пользователям
- **💬 Обратная связь** - получение сообщений от пользователей

## Команды администратора

```bash
/admin - админ панель
/broadcast <текст> - рассылка всем пользователям
/stats_full - подробная статистика
```

## Deep Linking

Бот поддерживает deep linking для прямых ссылок на фильмы:

```
https://t.me/your_bot_username?start=movie_123
```

## Мониторинг и логи

### Просмотр логов

```bash
# Логи бота в Docker
docker-compose logs -f telegram-bot

# Локальные логи
tail -f telegram-bot.log
```

### Основные метрики

- Количество пользователей
- Активность по дням
- Популярные команды
- Ошибки и исключения

## Безопасность

### Рекомендации

1. **Храните токен в секрете** - никогда не публикуйте его
2. **Ограничьте админов** - добавляйте только доверенных пользователей
3. **Мониторьте активность** - следите за подозрительными действиями
4. **Регулярные бэкапы** - сохраняйте статистику пользователей

### Защита от спама

```python
# В коде бота уже реализованы:
- Rate limiting для команд
- Фильтрация сообщений
- Блокировка подозрительных пользователей
```

## Масштабирование

### При росте пользователей

1. **База данных** - замените словарь на PostgreSQL
2. **Кэширование** - используйте Redis для быстрого доступа
3. **Очереди** - Celery для фоновых задач
4. **Мониторинг** - Prometheus + Grafana

### Пример миграции на БД

```python
# models.py
class TelegramUser(models.Model):
    telegram_id = models.BigIntegerField(unique=True)
    first_name = models.CharField(max_length=255)
    username = models.CharField(max_length=255, blank=True)
    first_visit = models.DateTimeField(auto_now_add=True)
    last_visit = models.DateTimeField(auto_now=True)
    visits_count = models.PositiveIntegerField(default=1)
```

## Troubleshooting

### Частые проблемы

1. **Бот не отвечает**
   - Проверьте токен
   - Убедитесь, что бот запущен
   - Проверьте логи на ошибки

2. **Web App не открывается**
   - Проверьте URL в переменных
   - Убедитесь, что сайт доступен по HTTPS
   - Проверьте настройки CORS

3. **Команды не работают**
   - Перезапустите бота
   - Проверьте права администратора
   - Обновите команды через /setcommands

### Полезные команды для отладки

```bash
# Проверка статуса бота
curl "https://api.telegram.org/bot<TOKEN>/getMe"

# Проверка webhook (если используется)
curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"

# Получение обновлений
curl "https://api.telegram.org/bot<TOKEN>/getUpdates"
```

## Дополнительные возможности

### Уведомления

```python
# Уведомления о новых фильмах
async def notify_new_movies():
    for user_id in get_all_users():
        await bot.send_message(
            user_id, 
            "🎬 Добавлены новые фильмы! Проверьте раздел 'Новинки'"
        )
```

### Персонализация

```python
# Рекомендации на основе истории
async def send_recommendations(user_id):
    recommendations = get_user_recommendations(user_id)
    await bot.send_message(
        user_id,
        f"🎯 Рекомендуем посмотреть: {recommendations}"
    )
```

### Интеграция с аналитикой

```python
# Отправка событий в Google Analytics
def track_user_action(user_id, action):
    # Код для отправки в GA
    pass
```